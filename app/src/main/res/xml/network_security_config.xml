<?xml version="1.0" encoding="utf-8"?>
<!--
  Allow cleartext to the typical Ollama host targets so a stock Ollama
  install on the same LAN / loopback works out of the box. Public IPs and
  HTTPS endpoints are unaffected: cleartext is opt-in, declared per-domain.

  If your Ollama runs on a different LAN host, add an extra <domain> for
  it (or front Ollama with HTTPS).
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">localhost</domain>
        <domain includeSubdomains="false">127.0.0.1</domain>
        <domain includeSubdomains="false">10.0.2.2</domain>
    </domain-config>
</network-security-config>
